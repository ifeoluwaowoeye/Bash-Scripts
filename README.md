# AWS High-Availabilty Project Bash Scripts

This repository contains the Bash scripts used in my AWS highh-availability web appllication project

## Scripts

- jump-server.sh - contains the set up commands used to create the jump/bastion server.
- web-server.sh - contains set up commands used to create the webservers.
